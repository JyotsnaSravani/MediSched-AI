"""
Django admin configuration for analytics app.
Sprint 4 - Analytics are computed on-the-fly, no models to register.
"""

from django.contrib import admin

# Analytics are computed from existing models
# No models to register
