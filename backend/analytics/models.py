"""
Analytics models (if needed for caching).
Sprint 4 - Most analytics are computed on-the-fly from existing models.
"""

from django.db import models

# Analytics are computed from existing models (Appointment, CallLog, ReminderLog, DoctorSlot)
# No additional models needed for Sprint 4
